

Remove the WhatsApp share button and its handler from `src/pages/Outstanding.tsx`:

1. Remove the `MessageCircle` icon import
2. Remove the `handleShareWhatsApp` callback
3. Remove the WhatsApp `<Button>` from the header

